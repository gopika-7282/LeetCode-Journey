class Solution:
    def findTheDifference(self, s: str, t: str) -> str:
        for i in t:
            if s.count(i) != t.count(i):
                return i

"""Problem:
        Given two strings s and t, where t is formed by shuffling s and adding
        one extra character, return that extra character.
#Traverse each character in t and compare its frequency in s and t.
# The character whose counts differ is the extra character added to t.
# Time Complexity: O(n²), Space Complexity: O(1)               
"""