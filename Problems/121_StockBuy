class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        profit=0
        mini=float("inf")
        for i in prices:
            if i<mini:
                mini=i
            elif i-mini>profit:
                profit=i-mini
        return profit            
            
            